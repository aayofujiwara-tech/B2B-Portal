# 運用マニュアル：リロード方式の運用ガイド

## 概要

本ポータルでは「限定管理・リロード」モデルを採用しています。
空室の総数を直接公開するのではなく、「今週の緊急受入枠」として限定数を表示し、
心理的な希少性を演出しながら営業プロセスを自動化します。

---

## 1. 基本的な運用フロー

### 週次リロード（推奨）

```
毎週月曜日の朝：
1. 管理画面（/admin）にアクセス
2. 「受入枠管理」タブを確認
3. 新しい総枠数（推奨：3〜5枠）を入力
4. 「枠をリロード」ボタンをクリック
```

### 枠が0になった場合

枠が全て使用されると、トップページのプログレスバーが自動的に
「次回の枠を調整中」ステータスに切り替わります。
MSWにはこの状態が表示され、次回のリロードを待つ形になります。

---

## 2. 管理画面の操作方法

### アクセス方法
- URL: `/admin`
- ナビゲーションバーの「管理画面」をクリック

### 受入枠管理タブ
- **現在の枠状況**: 総枠数、使用済み、残り、ステータスを一覧表示
- **プログレスバー**: 視覚的に枠の使用状況を確認
- **枠のリロード**: 新しい総枠数を設定して枠をリセット

### 判定ログタブ
- MSWが実行した全ての判定結果を時系列で確認
- 各判定の入力内容（疾患、ADL、性別、予算、希望時期）と結果を表示
- 「受入可能」「要相談」のステータスでフィルタリング可能

### 予約ログタブ
- 内覧予約の一覧を確認
- 施設名、担当者、希望日時、備考を確認

---

## 3. 推奨する枠数の設定

| 状況 | 推奨枠数 | 理由 |
|------|---------|------|
| 通常時 | 3〜5枠 | 適度な希少性を維持 |
| 繁忙期 | 2〜3枠 | 緊急性を強調 |
| 空室多数時 | 5〜7枠 | 受入体制をアピール |
| キャンペーン時 | 3枠 | 限定感を最大化 |

---

## 4. 判定ロジック

### 「受入可能」と判定される条件
- 人工呼吸器・透析以外の疾患
- 予算が8万円超

### 「要相談」と判定される条件
- 人工呼吸器または透析の方
- 予算が〜8万円の方

### 補足情報として付与されるもの
- ADL全介助の方：「訪問看護体制の確認が必要」
- 即日〜3日以内希望：「緊急対応枠での調整」

---

## 5. Analytics（分析）

### 現在の実装
ブラウザのコンソールに以下のイベントが記録されます：

```
[Analytics] 2025-XX-XX | slot_view | view_progress_bar | 3/5
[Analytics] 2025-XX-XX | assessment_start | select_disease | 認知症
[Analytics] 2025-XX-XX | assessment_submit | submit_form | 認知症
[Analytics] 2025-XX-XX | result_view | view_result | acceptable
[Analytics] 2025-XX-XX | pdf_download | click_download | facility_pamphlet
[Analytics] 2025-XX-XX | booking_submit | submit_booking | 2025-02-15
```

### 将来の拡張
`app/lib/analytics.ts` の `trackEvent` 関数を修正することで、
Google Analytics 4、Mixpanel、独自のログ収集APIなどに接続できます。

---

## 6. データの保存場所

現在のプロトタイプでは、全データは **localStorage** に保存されます。

| キー | 内容 |
|-----|------|
| `b2b_portal_slots` | 枠の設定（総数、使用数、ステータス） |
| `b2b_portal_assessment_logs` | 判定ログ |
| `b2b_portal_booking_logs` | 予約ログ |

**本番環境への移行時は、これらをサーバーサイドのデータベースに置き換えてください。**

---

## 7. 開発・起動方法

```bash
# 依存パッケージのインストール
npm install

# 開発サーバーの起動
npm run dev

# 本番ビルド
npm run build

# 本番サーバーの起動
npm start
```

開発サーバーは `http://localhost:3000` で起動します。
